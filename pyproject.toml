[project]
name = "vasya-assistant"
version = "0.1.0"
description = "Minimal MVP skeleton for Telegram bot with Bpium integration"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.1",
]

[tool.uv]
dev-dependencies = []
